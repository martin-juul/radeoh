<?xml version="1.0" encoding="utf-8" ?>

<ContentPage
    x:Class="Radeoh.Views.MainPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:Radeoh.ViewModels;assembly=Radeoh"
    xmlns:m="clr-namespace:Radeoh.Models;assembly=Radeoh"
>
    <ContentPage.Content>
        <ListView 
            ItemsSource="{Binding Stations}"
            Margin="0,20,0,0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Title}" TextColor="White" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>